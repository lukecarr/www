<!DOCTYPE html>
<html lang="en">
    <% include!("./head.stpl"); %>
    <body>
        <% include!("./header.stpl"); %>
        <h2>links</h2>
        <ul>
            <% for link in &links { %>
                <li><a href="<%= link.href %>" target="_blank" rel="noopener"><%= link.text %></a></li>
            <% } %>
        </ul>
        <% include!("./footer.stpl"); %>
    </body>
</html>